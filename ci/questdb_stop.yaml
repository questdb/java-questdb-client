steps:
  - bash: |
      echo "Stopping QuestDB server..."
      ps aux | awk '/io\.questdb\.ServerMain/ && !/awk/ {print $2}' | xargs -r kill -TERM
      rm -rf questdb-data
    displayName: "Stop QuestDB server"
